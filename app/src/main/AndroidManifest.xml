<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.controledecontas">

    <application
        android:allowBackup="true"
        android:icon="@drawable/calculadora"
        android:label="@string/app_name"
        android:roundIcon="@drawable/calculadora"
        android:supportsRtl="true"
        android:theme="@style/Theme.ControleDeContas">
        <activity android:name=".BalancoMensal"></activity>
        <activity android:name=".AdicionarPagamento" />
        <activity android:name=".ConsultarDespesas" />
        <activity android:name=".AdicionarDespesa" />
        <activity android:name=".Funcionalidades" />
        <activity android:name=".Login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>